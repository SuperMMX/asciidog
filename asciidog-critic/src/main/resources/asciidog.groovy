import org.supermmx.asciidog.critic.*

asciidog {
    // the name of the plugin
    name = 'asciidog-critic'

    // the customized plugin loader, optioinal
    suite = CriticPluginSuite

    backends = [
    ]

    // the plugins
    plugins = [
    ]

    builders = [
    ]
}
